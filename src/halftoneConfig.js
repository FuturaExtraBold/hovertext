export const halftoneConfig = {
  gridSize: 10,        // Sample block size in pixels
  maxDotSize: 10,      // Max circle diameter (px)
  minDotSize: 0,       // Min circle diameter (px)
  dotColor: '#ffffff', // Circle fill color
  brightnessMin: 0,    // Brightness floor (0-255)
  brightnessMax: 255,  // Brightness ceiling (0-255)
  animationDuration: 0.5, // GSAP tween duration (seconds)
  animationEase: 'power2.out', // GSAP easing
};
